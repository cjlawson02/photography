-- Phase 0 skeleton migration (FamilyNotes-style flat SQL under src/db/migrations).
-- Portfolio vs review domains are separate; no shared photos table.
-- Exact DDL / columns are _TBD_ — see docs/HLD.md.
-- This file establishes the migration stream; domain tables arrive in later migrations.

SELECT 1;
