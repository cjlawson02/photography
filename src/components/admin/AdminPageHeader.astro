---
interface Props {
	title: string;
	lede?: string;
}

const { title, lede } = Astro.props;
---

<header class="admin-page-header">
	<h1 class="admin-page-title">{title}</h1>
	{lede ? <p class="admin-lede">{lede}</p> : null}
</header>
